{
  "name": "rcba-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix RCBA start",
    "build": "npm --prefix RCBA run build",
    "test": "npm --prefix RCBA test",
    "watch": "npm --prefix RCBA run watch"
  }
}
